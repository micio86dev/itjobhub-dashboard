import { test, expect } from '@playwright/test';

test.describe('Jobs Map', () => {
    test('mappa caricata (div mappa presente)', async ({ page }) => {
        // TODO
    });

    test('filtro remote aggiorna contatore visibile', async ({ page }) => {
        // TODO
    });

    test('reset filtri -> contatore torna al totale', async ({ page }) => {
        // TODO
    });
});
