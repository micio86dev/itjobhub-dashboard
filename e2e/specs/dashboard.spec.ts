import { test, expect } from '@playwright/test';

test.describe('Dashboard Page', () => {
    test('overview carica 4 StatCard con valori > 0', async ({ page }) => {
        // TODO
    });

    test('grafici presenti nel DOM (canvas o svg)', async ({ page }) => {
        // TODO
    });

    test('navigazione sidebar -> URL e titolo pagina corretti', async ({ page }) => {
        // TODO
    });
});
