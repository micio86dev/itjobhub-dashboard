import { test, expect } from '@playwright/test';

test.describe('Theme Toggle', () => {
    test('toggle -> classe dark su HTML', async ({ page }) => {
        // TODO
    });

    test('cookie admin-theme impostato', async ({ page }) => {
        // TODO
    });

    test('reload -> tema persistito dal cookie', async ({ page }) => {
        // TODO
    });
});
