import { describe, it, expect } from 'vitest'

describe('JobsMap', () => {
    describe('mount', () => {
        it.todo('Google Maps caricato, div mappa presente')
    })

    describe('filtri', () => {
        it.todo('cambio filtro aggiorna contatore marker')
    })

    describe('cluster', () => {
        it.todo('click su cluster mostra lista job')
    })
})
