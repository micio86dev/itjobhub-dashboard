import { describe, it, expect } from 'vitest'

describe('useAuth', () => {
    it.todo('login con credenziali valide aggiorna store')
    it.todo('login con credenziali errate setta error')
    it.todo('logout pulisce token e user')
    it.todo('token mancante -> isAuthenticated = false')
})
