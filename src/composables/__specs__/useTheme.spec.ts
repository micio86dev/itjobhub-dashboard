import { describe, it, expect } from 'vitest'

describe('useTheme', () => {
    it.todo('legge cookie al mount')
    it.todo('fallback a system preference se nessun cookie')
    it.todo('toggle scrive cookie e applica classe dark')
    it.todo('ricarica dalla stessa preferenza dopo toggle')
})
