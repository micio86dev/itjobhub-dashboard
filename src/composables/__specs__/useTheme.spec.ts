import { describe, it, expect } from 'vitest'

describe('useTheme', () => {
    it('stub', () => { expect(1).toBe(1); })
    it('stub', () => { expect(1).toBe(1); })
    it('stub', () => { expect(1).toBe(1); })
    it('stub', () => { expect(1).toBe(1); })
})
