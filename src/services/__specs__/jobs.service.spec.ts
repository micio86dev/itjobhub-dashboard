import { describe, it, expect } from 'vitest'

describe('jobs.service', () => {
    it.todo('getJobs chiama endpoint corretto con params')
    it.todo('getJobsWithLocation filtra solo jobs con GPS')
    it.todo('getTopSkills ritorna array ordinato per count')
})
